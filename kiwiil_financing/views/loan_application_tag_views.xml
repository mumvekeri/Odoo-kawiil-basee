<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Loan Application Tag List View -->
    <record id="view_loan_application_tag_list" model="ir.ui.view">
        <field name="name">loan.application.tag.list</field>
        <field name="model">loan.application.tag</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <!-- Loan Application Tag Form View -->
    <record id="view_loan_application_tag_form" model="ir.ui.view">
        <field name="name">loan.application.tag.form</field>
        <field name="model">loan.application.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Loan Application Tag Action -->
    <record id="action_loan_application_tag" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">loan.application.tag</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>